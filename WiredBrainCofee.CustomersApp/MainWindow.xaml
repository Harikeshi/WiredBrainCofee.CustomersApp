<Window x:Class="WiredBrainCofee.CustomersApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-nam pace:WiredBrainCofee.CustomersApp"
        mc:Ignorable="d"
        Title="MainWindow"
        d:DesignWidth="500" d:DesignHeight="600" FontSize="20">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>

        <!-- Main menu -->
        <Menu Grid.ColumnSpan="2" FontSize="20">
            <MenuItem Header="_View">
                <MenuItem Header="_Customers"/>
                <MenuItem Header="_Products"/>
            </MenuItem>
        </Menu>

        <!-- Header -->
        <!-- Grid для окрашивания -->
        <Grid Grid.Row="1" Grid.ColumnSpan="2" Background="#FF7F27">
            <StackPanel HorizontalAlignment="Center" Orientation="Horizontal">
                <Image Source="/Images/logo.png" Width="100" Margin="5"/>
                <TextBlock Text="Customers App" FontSize="30"
                       Foreground="White"
                       VerticalAlignment="Center"></TextBlock>
                <TextBlock Text="Version 1.0" FontSize="16"
                       VerticalAlignment="Center" Margin="10 0 0 22"
           Foreground="#333333"/>
            </StackPanel>
        </Grid>

        <!-- Customer list -->
        <StackPanel Grid.Row="2"  Background="#777">
            <StackPanel Orientation="Horizontal"  Background="#777">
                <Button Margin="10" Width="75" Background="#FF7F27">
                    <StackPanel Orientation="Horizontal">
                        <Image Source="/Images/add.png" Height="18" Margin="0 0 5 0"/>
                        <TextBlock Text="Add"/>
                    </StackPanel>
                </Button>
                <Button Content="Delete" Width="75" Margin="0 10 10 10" Background="#FF7F27"
                        HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>
                <Button Margin="0 10 10 10"  Background="#FF7F27">
                    <Image Source="/Images/move.png" Height="18"/>
                </Button>
            </StackPanel>
            <ListView Margin="10 0 10 10">
                <ListViewItem>Julia</ListViewItem>
                <ListViewItem>Alex</ListViewItem>
                <ListViewItem>Thomas</ListViewItem>
            </ListView>
        </StackPanel>

        <!-- Customer Detail -->
        <StackPanel Grid.Row="2" Grid.Column="1" Margin="10">
            <Label>Firstname:</Label>
            <TextBox/>
            <Label>Lastname:</Label>
            <TextBox/>
            <CheckBox Margin="0 10 0 0">
                Is developer
            </CheckBox>
        </StackPanel>

        <!-- Statusbar -->
        <StatusBar Grid.Row="3" Grid.ColumnSpan="2">
            <StatusBarItem FontSize="20" Content="(c) Wired Brain Coffee"/>
        </StatusBar>
    </Grid>
</Window>
